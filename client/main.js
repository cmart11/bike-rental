

const addToCart = (event) => {
    console.log(hohoh);
};

module.exports = addToCart;

// const buttons = document.getElementsByClassName('btn');

// buttons.addEventListener('click', (e) => {
//     let product = products.filter(el => el.name === e.target.name)[0];
//     // make POST to db
//     console.log('iiji');
// });
